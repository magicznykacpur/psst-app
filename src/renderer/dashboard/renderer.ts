const initDashboardRenderer = () => {
  window.addEventListener("DOMContentLoaded", () => {
  })
}


const apiURL = "http://localhost:42069/api"
const chats = `${apiURL}/chats`
const messages = `${apiURL}/messages`

initDashboardRenderer()